<?xml version="1.0" encoding="utf-8"?>
<project name="Stitches" basedir="." default="prod">


    <description>ANT build file for git://github.com/draeton/stitches</description>


    <property environment="env"/>
    <property file="build.properties"/>
    <property file="${common.properties}"/>
    <import file="${common.targets}"/>


    <target name="-init" description="Starting new build">
        <echo>Starting new build</echo>

        <fileset id="src.js" dir="${src.dir}/js">
            <include name="**/*.js"/>
        </fileset>

        <filelist id="src.css" dir="${src.dir}">
            <file name="${repo}.css"/>
            <file name="toolbar.css"/>
            <file name="palette.css"/>
            <file name="sprite.css"/>
        </filelist>
    </target>


    <target name="-copy-depends" depends="-init" description="Copying dependencies">
        <echo>Copying dependencies IMPORTANT</echo>

        <copy todir="${build.dir}/img">
            <fileset dir="${src.dir}/img"/>
        </copy>
        <copy todir="${build.dir}/lib">
            <fileset dir="${src.dir}/lib"/>
        </copy>
    </target>


    <!-- * build * -->
    <target name="prod"
        description="Builds project files for production use"
        depends="
            -init,
            -clean,
            validate,
            -increment,
            -readme,
            docco,
            require,
            -concat,
            -minify,
            -copy-depends,
            -zip,
            commit">
    </target>

    <target name="pages"
        description="Builds project pages (and prod)"
        depends="
            prod,
            -gh-pages-update,
            -gh-pages-commit">
    </target>


</project>